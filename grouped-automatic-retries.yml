steps:
- group: "Group 1"
  steps:
  - command: |
      if [[ -n "$$BUILDKITE_RETRY_COUNT" && "$$BUILDKITE_RETRY_COUNT" -gt 1 ]]; then
        echo "Pass"
        exit 0
      else
        sleep 20
        echo "Fail"
        exit 1
      fi
    label: "Fails but eventually passes"
    retry:
      automatic:
      - exit_status: -1
        limit: 3
      - exit_status: 1
        limit: 3
      - exit_status: 255
        limit: 3
